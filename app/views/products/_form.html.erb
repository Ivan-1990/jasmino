<%= form_for(@product, :class => 'form-horizontal') do |f| %>


    <%= f.label :name %>
    <%= f.text_field :name %>
	
	<%= f.label :price %>
	<%= f.text_field :price %>
	
    <div class="actions">
    <%= f.submit 'Отправить', :class => 'btn' %>
  </div>
<% end %>
